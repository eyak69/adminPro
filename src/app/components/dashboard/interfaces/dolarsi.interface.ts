export interface DolarSi {
    casa: {
      compra: string;
      venta: string;
      agencia: string;
      nombre: string;
      variacion: string;
      ventaCero: string;
      decimales: string;
      mejor_compra?: boolean;
      mejor_venta?: boolean;
      fecha?: string;
      recorrido?: string;
      afluencia?: any;
      observaciones?: any;
    };
  }